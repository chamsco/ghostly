/**
 * Onboarding Page
 * 
 * Displays the onboarding flow for new users after registration.
 * Provides a guided introduction to the application's key features.
 */
import { OnboardingFlow } from '@/features/onboarding/components/OnboardingFlow';

export function Onboarding() {
  return <OnboardingFlow />;
} 